# =============================================================================
# TARJETA CRC - tests/e2e/.env.e2e.example (Plantilla E2E determinista)
# =============================================================================
# Responsabilidades:
# - Proveer valores de E2E no sensibles y reproducibles.
# - Evitar dependencias de credenciales reales.
#
# Colaboradores:
# - scripts/e2e/run_e2e.sh
# - tests/e2e/playwright.config.ts
#
# Invariantes:
# - No incluir secretos reales.
# =============================================================================

# Credenciales E2E (solo local/CI, NO usar en prod)
E2E_ADMIN_EMAIL=admin@example.com
E2E_ADMIN_PASSWORD=admin-pass-123

# Base URLs (compose)
E2E_BASE_URL=http://localhost:3000
E2E_API_URL=http://localhost:8000
E2E_USE_COMPOSE=1

# API key E2E (no sensible)
API_KEYS_CONFIG={"e2e-key":["ingest","ask"]}
TEST_API_KEY=e2e-key

# Modo fake (evita llamadas externas)
FAKE_LLM=1
FAKE_EMBEDDINGS=1
