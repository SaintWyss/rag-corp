# =============================================================================
# TARJETA CRC - tests/e2e/.env.example (Plantilla E2E)
# =============================================================================
# Responsabilidades:
# - Documentar variables minimas para ejecutar Playwright E2E.
# - Proveer placeholders seguros (sin credenciales reales).
#
# Colaboradores:
# - tests/e2e/README.md
# - tests/e2e/playwright.config.ts
#
# Invariantes:
# - No incluir secretos reales.
# =============================================================================

# Credenciales admin para login E2E (placeholder)
E2E_ADMIN_EMAIL=admin@example.com
E2E_ADMIN_PASSWORD=CHANGE_ME

# Base URLs (si no usas compose)
E2E_BASE_URL=http://localhost:3000
E2E_API_URL=http://localhost:8000

# Si usas docker compose para levantar stack E2E
E2E_USE_COMPOSE=1

# API key opcional para sessionStorage (si aplica)
TEST_API_KEY=
