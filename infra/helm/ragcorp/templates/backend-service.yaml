# =============================================================================
# TARJETA CRC - infra/helm/ragcorp/templates/backend-service.yaml
# =============================================================================
# Responsabilidades:
# - Exponer el backend via Service estable.
# - Mantener selector/puerto alineado al Deployment.
#
# Colaboradores:
# - templates/backend-deployment.yaml
# - templates/ingress.yaml
#
# Invariantes:
# - Selector debe coincidir con labels del backend.
# =============================================================================

{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ragcorp.componentName" (dict "root" . "component" "backend") }}
  labels:
    {{- include "ragcorp.componentLabels" (dict "root" . "component" "backend") | nindent 4 }}
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "ragcorp.selectorLabels" (dict "root" . "component" "backend") | nindent 4 }}
{{- end }}
