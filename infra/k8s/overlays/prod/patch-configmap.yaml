# =============================================================================
# TARJETA CRC - infra/k8s/overlays/prod/patch-configmap.yaml
# =============================================================================
# Responsabilidades:
# - Ajustar toggles de entorno para producción.
# - Mantener valores no sensibles.
#
# Colaboradores:
# - infra/k8s/base/configmap.yaml
#
# Invariantes:
# - No incluir secretos.
# =============================================================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: ragcorp-config
  namespace: ragcorp
data:
  APP_ENV: "production"
  ALLOWED_ORIGINS: "https://ragcorp.example.com"
