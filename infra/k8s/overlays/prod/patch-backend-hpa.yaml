# =============================================================================
# TARJETA CRC - infra/k8s/overlays/prod/patch-backend-hpa.yaml
# =============================================================================
# Responsabilidades:
# - Ajustar rangos de escalado para producción.
#
# Colaboradores:
# - infra/k8s/base/backend-hpa.yaml
#
# Invariantes:
# - Mantener métricas base (CPU/memory).
# =============================================================================

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ragcorp-backend-hpa
  namespace: ragcorp
spec:
  minReplicas: 3
  maxReplicas: 12
