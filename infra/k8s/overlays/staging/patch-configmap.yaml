# =============================================================================
# TARJETA CRC - infra/k8s/overlays/staging/patch-configmap.yaml
# =============================================================================
# Responsabilidades:
# - Ajustar toggles de entorno para staging.
# - Mantener valores no sensibles.
#
# Colaboradores:
# - infra/k8s/base/configmap.yaml
#
# Invariantes:
# - No incluir secretos.
# =============================================================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: ragcorp-config
  namespace: ragcorp
data:
  APP_ENV: "staging"
  ALLOWED_ORIGINS: "https://staging.ragcorp.example.com"
