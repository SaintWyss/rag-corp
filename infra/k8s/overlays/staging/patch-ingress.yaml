# =============================================================================
# TARJETA CRC - infra/k8s/overlays/staging/patch-ingress.yaml
# =============================================================================
# Responsabilidades:
# - Ajustar hosts y TLS para staging.
#
# Colaboradores:
# - infra/k8s/ingress.yaml
#
# Invariantes:
# - Mantener paths coherentes con backend/frontend.
# =============================================================================

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ragcorp-ingress
spec:
  tls:
    - hosts:
        - staging.ragcorp.example.com
        - api.staging.ragcorp.example.com
      secretName: ragcorp-staging-tls
  rules:
    - host: staging.ragcorp.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ragcorp-frontend
                port:
                  name: http
    - host: api.staging.ragcorp.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ragcorp-backend
                port:
                  name: http
