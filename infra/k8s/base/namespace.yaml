# RAG Corp Kubernetes Namespace
# =============================================================================
# TARJETA CRC - infra/k8s/base/namespace.yaml
# =============================================================================
# Responsabilidades:
# - Definir el namespace de trabajo para RAG Corp.
#
# Colaboradores:
# - infra/k8s/base/kustomization.yaml
#
# Invariantes:
# - El nombre del namespace debe ser estable.
# =============================================================================

apiVersion: v1
kind: Namespace
metadata:
  name: ragcorp
  labels:
    app.kubernetes.io/name: ragcorp
    app.kubernetes.io/part-of: ragcorp
