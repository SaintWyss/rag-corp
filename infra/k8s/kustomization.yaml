# =============================================================================
# TARJETA CRC - infra/k8s/kustomization.yaml
# =============================================================================
# Responsabilidades:
# - Mantener compatibilidad con "kubectl apply -k infra/k8s/".
# - Delegar al base Kustomize real en infra/k8s/base/.
#
# Colaboradores:
# - infra/k8s/base/kustomization.yaml
#
# Invariantes:
# - No incluir secretos.
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - base
