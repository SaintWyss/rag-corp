---
applyTo: "doc/**"
---

- No afirmar features no verificadas: si no existe en el repo, marcar TODO/Planned.
- Endpoints: siempre alineados con `routes.py` + `main.py`.
- Docker: comandos alineados con `compose.yaml` y servicios reales.
- Mantener `doc/README.md` como índice y enlaces válidos.
- Mermaid en `doc/diagrams/*.md` para flujos importantes.
