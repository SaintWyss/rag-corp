# ==============================================================================
# RAG Corp - Variables de Entorno
# ==============================================================================
# Copia este archivo a .env y completa los valores:
#   cp .env.example .env
#
# NUNCA commitees .env (contiene secretos)
# ==============================================================================

# ------------------------------------------------------------------------------
# Google Gemini API (REQUERIDO)
# ------------------------------------------------------------------------------
# Obtener en: https://aistudio.google.com/app/apikey
# Usado para: embeddings (text-embedding-004) + LLM (gemini-1.5-flash)
GOOGLE_API_KEY=your-google-api-key-here

# ------------------------------------------------------------------------------
# Base de Datos (REQUERIDO)
# ------------------------------------------------------------------------------
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Default para Docker Compose local:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rag

# ------------------------------------------------------------------------------
# CORS - Orígenes Permitidos (OPCIONAL)
# ------------------------------------------------------------------------------
# Lista separada por comas de URLs permitidas para requests cross-origin
# Default: http://localhost:3000 (frontend en desarrollo)
# Ejemplo producción: https://app.ragcorp.com,https://admin.ragcorp.com
ALLOWED_ORIGINS=http://localhost:3000
