# =============================================================================
# TARJETA CRC - deploy/tenants/company-b/prod/values.yaml
# =============================================================================
# Responsabilidades:
# - Valores Helm para company-b en produccion.
# - Definir imagenes, ingress y namespace.
#
# Colaboradores:
# - infra/helm/ragcorp/values.yaml
#
# Invariantes:
# - No incluir secretos reales.
# =============================================================================

namespace:
  create: true
  name: ragcorp-company-b-prod

config:
  data:
    APP_ENV: "production"
    ALLOWED_ORIGINS: "https://company-b.prod.example.com"

backend:
  replicaCount: 3
  image:
    repository: ragcorp/backend
    tag: "latest"

worker:
  replicaCount: 2

frontend:
  replicaCount: 3
  image:
    repository: ragcorp/frontend
    tag: "latest"

ingress:
  enabled: true
  hosts:
    - host: company-b.prod.example.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
    - host: api.company-b.prod.example.com
      paths:
        - path: /
          pathType: Prefix
          service: backend
  tls:
    - secretName: ragcorp-company-b-prod-tls
      hosts:
        - company-b.prod.example.com
        - api.company-b.prod.example.com

autoscaling:
  backend:
    enabled: true
    minReplicas: 3
    maxReplicas: 12

pdb:
  enabled: true

networkPolicy:
  enabled: true

secrets:
  existingSecret: ragcorp-company-b-prod
