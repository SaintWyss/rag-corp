# =============================================================================
# TARJETA CRC - deploy/tenants/company-a/staging/values.yaml
# =============================================================================
# Responsabilidades:
# - Valores Helm para company-a en staging.
# - Definir imagenes, ingress y namespace.
#
# Colaboradores:
# - infra/helm/ragcorp/values.yaml
#
# Invariantes:
# - No incluir secretos reales.
# =============================================================================

namespace:
  create: true
  name: ragcorp-company-a-staging

config:
  data:
    APP_ENV: "staging"
    ALLOWED_ORIGINS: "https://company-a.staging.example.com"

backend:
  image:
    repository: ragcorp/backend
    tag: "staging"

frontend:
  image:
    repository: ragcorp/frontend
    tag: "staging"

ingress:
  enabled: true
  hosts:
    - host: company-a.staging.example.com
      paths:
        - path: /
          pathType: Prefix
          service: frontend
    - host: api.company-a.staging.example.com
      paths:
        - path: /
          pathType: Prefix
          service: backend
  tls:
    - secretName: ragcorp-company-a-staging-tls
      hosts:
        - company-a.staging.example.com
        - api.company-a.staging.example.com

secrets:
  existingSecret: ragcorp-company-a-staging
